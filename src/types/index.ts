export interface Category {
  name: string;
  emoji: string;
  color: string;
}

export interface Product {
  id: number;
  name: string;
  price: string;
  image: string;
  badge: string;
  badgeColor: string;
}
